#

# Recommended minimum configuration:
#
#acl manager proto cache_object
#acl localhost src 127.0.0.1/32
#acl to_localhost dst 127.0.0.0/8 0.0.0.0/32

# Example rule allowing access from your local networks.
# Adapt to list your (internal) IP networks from where browsing
# should be allowed
#acl localnet src 10.0.0.0/8	# RFC1918 possible internal network
#acl localnet src 172.16.0.0/12	# RFC1918 possible internal network
#acl localnet src 192.168.0.0/16	# RFC1918 possible internal network
#acl localnet src fc00::/7       # RFC 4193 local private network range
#acl localnet src fe80::/10      # RFC 4291 link-local (directly plugged) machines
acl all src all
acl SSL_ports port 443
acl Safe_ports port 80		# http
acl Safe_ports port 21		# ftp
acl Safe_ports port 443		# https
acl Safe_ports port 70		# gopher
acl Safe_ports port 210		# wais
acl Safe_ports port 1025-65535	# unregistered ports
acl Safe_ports port 280		# http-mgmt
acl Safe_ports port 488		# gss-http
acl Safe_ports port 591		# filemaker
acl Safe_ports port 777		# multiling http
acl CONNECT method CONNECT
http_access allow CONNECT SSL_ports
acl purge method PURGE
#
# Recommended minimum Access Permission configuration:
#
# Only allow cachemgr access from localhost
acl webserver src 172.16.0.1
http_access allow manager localhost
http_access allow manager webserver
http_access deny manager

# Deny requests to certain unsafe ports
http_access deny !Safe_ports

# Deny CONNECT to other than secure SSL ports
http_access deny CONNECT !SSL_ports

# We strongly recommend the following be uncommented to protect innocent
# web applications running on the proxy server who think the only
# one who can access services on "localhost" is a local user
http_access deny to_localhost

#
# INSERT YOUR OWN RULE(S) HERE TO ALLOW ACCESS FROM YOUR CLIENTS
#

# Example rule allowing access from your local networks.
# Adapt localnet in the ACL section to list your (internal) IP networks
# from where browsing should be allowed
http_access allow localhost
http_access allow purge localhost
http_access deny purge
url_rewrite_access deny localhost

#header_access Via deny all

acl nord dst 172.16.0.41
always_direct allow nord

acl esx src 172.16.0.23
acl freepbx src 172.16.0.63
acl msssql src 172.16.0.24
acl hz src 172.16.13.200
acl esx_domain dstdomain "/etc/squid/esx_domain.txt"
acl our_networks src 172.16.0.1-172.16.15.255
acl buhintbank src 192.168.253.2-192.168.253.255
acl ni_network src 172.16.16.1-172.16.255.255
acl buh_n44 src 172.17.50.1-172.17.50.254
acl ok_n44 src 172.17.51.1-172.17.51.5
acl magaz src 192.168.0.0/16
acl tr_domain dstdomain "/etc/squid/tr_domain.txt"
acl magaz_domain dstdomain "/etc/squid/magaz.txt"
#acl our_domain dstdomain "/etc/squid/our_domain.txt"
acl nobanners src 0/0
#acl banners url_regex "/etc/squid/banners.txt"
http_access allow freepbx all
http_access allow msssql all
http_access deny hz all
#http_access deny nobanners banners
http_access allow tr_domain ni_network
http_access allow tr_domain buh_n44
http_access allow magaz_domain magaz
#http_access allow our_domain ni_network
http_access allow esx_domain esx
http_access allow our_networks !esx
http_access allow our_networks
http_access allow ok_n44
http_access allow buhintbank

forwarded_for on
cache deny all

# And finally deny all other access to this proxy
http_access deny all

cache_effective_user _squid
cache_effective_group _squid
cache_mem 640 MB
memory_pools on
reload_into_ims on


dns_nameservers 172.16.0.9

# Squid normally listens to port 3128
http_port 172.16.0.1:3129
http_port 172.16.0.1:3128 transparent

logfile_rotate 10
error_directory /usr/local/share/squid/errors/ru

# We recommend you to use at least the following line.
hierarchy_stoplist cgi-bin ?

# Uncomment and adjust the following to add a disk cache directory.
cache_dir null /tmp
#TEMTTA
cache_peer 172.16.0.16 parent 3128 3130 no-query no-digest
never_direct allow all

# Leave coredumps in the first cache dir
coredump_dir /var/squid/cache
cache_log /var/squid/logs/cache.log
access_log /var/squid/logs/access.log squid
cache_store_log none

# Add any of your own refresh_pattern entries above these.
refresh_pattern ^ftp:		1440	20%	10080
refresh_pattern ^gopher:	1440	0%	1440
refresh_pattern -i (/cgi-bin/|\?) 0	0%	0
refresh_pattern .		0	20%	4320

pid_filename /var/run/squid.pid